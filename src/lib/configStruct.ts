export interface MasterExport {
    version:number
    gitData:string
    configData:string
    elasticData:string
}

export interface config{
    gitUrl1:string
    gitUrl2:string
    mapClientId:string
}